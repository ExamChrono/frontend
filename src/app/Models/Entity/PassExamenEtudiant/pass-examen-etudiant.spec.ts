import { PassExamenEtudiant } from './pass-examen-etudiant';

describe('PassEsamenEtudiant', () => {
  it('should create an instance', () => {
    expect(new PassExamenEtudiant()).toBeTruthy();
  });
});
